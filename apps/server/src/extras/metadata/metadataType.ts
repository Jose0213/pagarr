/** Ported from NzbDrone.Core/Extras/Metadata/MetadataType.cs. Stored as an integer in MetadataFiles.Type. */
export enum MetadataType {
  Unknown = 0,
  AuthorMetadata = 1,
  BookMetadata = 2,
  AuthorImage = 3,
  BookImage = 4,
}
